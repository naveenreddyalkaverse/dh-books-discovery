'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

class ExtensibleError extends Error {
    constructor(statusCode, errorCode, message, data) {
        super(message);

        this._statusCode = data._statusCode || statusCode;
        this._errorCode = data._errorCode || errorCode;
        this._status = 'ERROR';
        this.message = message;

        if (data && _lodash2.default.isObject(data)) {
            (0, _lodash2.default)(data).keys().forEach(key => {
                this[key] = data[key];
            });
        } else {
            this.data = data;
        }

        Error.captureStackTrace(this, this.constructor.name);
    }
}
exports.default = ExtensibleError;