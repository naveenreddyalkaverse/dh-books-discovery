'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

var _redis = require('redis');

var _redis2 = _interopRequireDefault(_redis);

var _redisSentinel = require('redis-sentinel');

var _redisSentinel2 = _interopRequireDefault(_redisSentinel);

var _bluebird = require('bluebird');

var _bluebird2 = _interopRequireDefault(_bluebird);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function promisify(client) {
    return _bluebird2.default.promisifyAll(client);
}

exports.default = _lodash2.default.once(config => {
    const redisSentinelConfig = config.redisSentinelConfig;
    if (redisSentinelConfig) {
        return promisify(_redisSentinel2.default.createClient(redisSentinelConfig.endpoints, redisSentinelConfig.name));
    }

    const redisConfig = config.redisConfig;
    if (redisConfig) {
        return promisify(_redis2.default.createClient(redisConfig));
    }

    return promisify(_redis2.default.createClient());
});